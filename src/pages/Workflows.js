import React from 'react';
import WorkflowsComponent from '../components/Workflows'; // Fixed: Added 's' to match the actual file name

const Workflows = () => {
  return (
    <div>
      <WorkflowsComponent />
    </div>
  );
};

export default Workflows;