import React from 'react';
import VisualizedMetricsComponent from '../components/VisualizedMetrics';

const VisualizedMetrics = () => {
  return (
    <div>
      <VisualizedMetricsComponent />
    </div>
  );
};

export default VisualizedMetrics;